# 牛马的个人工作台 · 交付说明

**访问链接（电脑 / 手机通用）：**
https://8fb5da19b0264745bb5e1d8d492b56e8.bj8.agentos-app.net

> 本地手动数据（待办 / 资讯 / 灵感 / 出入·兜底）保存在浏览器 localStorage，换设备/清缓存前先到「我的」导出 JSON 备份。
> V9.1 物管数据（库存/出入/仓储费/查询追踪/出入库明细）来自 `data.json`，自动加载 + 可点「刷新数据」重新拉取。

## 版本说明

- **v0.6（当前）· 出入库本周/本月明细接入 V9.1**：「V9.1 物管数据看板 → 出入库」子 tab 新增明细表，列含日期、方向、项目号、合同号、型号/产品、仓库、数量、金额、单价，**本周(最近 7 天) / 本月** 一键切换；方向标签按国内习惯（入绿 / 出红），合同号按项目号精确关联 V9.1 合同详情（命中率 100%）。同时 `gen_data.py` 重写为**全自包含**（直接扫描 tool-results 自动识别各 sheet，去重/分类/聚合一体化），刷新数据按钮重跑即用。
- v0.5 · 新增 V9.1 物管数据看板 4 子板块（库存货值 / 出入库聚合 / 仓储费 / 查询追踪），自动加载 data.json。
- v0.4 · 新增「出入库 · 本周」手动登记板块（作为 V9.1 端不可直拉时的兜底）。
- v0.3 · 黄橙多巴胺配色上线。
- v0.2 / v0.1 已废弃。

## V9.1 物管数据看板 · 字段与来源

| 子板块 | 数据源 | 关键指标（截至 2026-08-31 拉取） |
|---|---|---|
| 库存货值 | 合同详情（758 条） | 库存总额 ¥17.96亿（25,209,920 件） · 12 个仓库 · 已完结 408 / 待完结 350 / 逾期 195 |
| 出入库 · 聚合 | 出入库明细（1300 条） | 总入库 ¥55.79亿 · 总出库 ¥37.82亿 · 16 个月 · 24 个仓库 · 月度柱状图 + 仓库出入 TOP12 |
| **出入库 · 明细** | 出入库逐笔 + 合同详情关联 | 本周(滚动 7 天) **8 笔**（入 ¥1652.0万 / 出 ¥3720.3万）· 本月 97 笔（入 ¥3.14亿 / 出 ¥6.40亿）· **合同号 100% 命中** · 型号填充 62%（剩余 38% 是 V9.1 录入时未填产品字段） |
| 仓储费 | 仓储费（73 条） | 合计 ¥146.2万 · 6 家承运商 · 顺丰运输明细 22 条 · 14 个计费月份 |
| 查询追踪 | 查询统计（sheet 5，365 条） | 已查 5 / 未查 293 · 覆盖率 1.7% · 重点未查清单 40 条 |

## V9.1 数据如何刷新

1. **重新拉取金山文档**：用 kdocs MCP 把合同详情 / 出入库 / 仓储费 / 查询统计四个 sheet 拉到 `tool-results/`（自动按字段特征分类）。sheet3 出入库需翻页（>1000 条），page_token 翻到底；sheet34 仓储费单页够。
2. **生成本地 data.json**：执行 `python gen_data.py`（已重写为自包含，无需中间 json 文件）。脚本自动：
   - sheet 3 按**记录 id 去重**（防止新旧拉取重复导致出入总额翻倍）
   - sheet 5 与 sheet 22 全未查清单**严格区分**（仅含「已查询」记录的文件才算查询源，否则会被误合并稀释覆盖率）
   - sheet 2 项目号 → 销售合同号建映射，**精确匹配 282/284 命中率**
   - 按**滚动 7 天 + 当前月**两个窗口生成明细事件（方向/日期/项目号/合同号/型号/仓库/数量/金额/单价）
3. **部署**：把更新后的 `dist/`（含新 data.json）重新部署到 CloudStudio。
4. **用户侧**：打开页面后自动 `fetch('data.json?t=…')`；点看板顶部的「刷新数据」按钮可立刻重新拉取，失败时回退到 localStorage 缓存（key `wb_v9_data`）。

> 说明：纯静态页面没有后端，不能直连金山文档。已尽量做了缓存兜底（localStorage）+ 缓存破坏（`?t=Date.now()`），保证多人多设备看到的是最新部署版。

## 页面更新与离线缓存（重要）

Service Worker 采用「**网络优先**」策略（缓存名 `gzt-v2`）：

- **页面 `index.html` 与数据 `data.json`** → 网络优先：每次打开/刷新都拉最新，离线时自动回退缓存。
- **图标 / manifest 等静态资源** → 缓存优先，省流量。
- **新版本自动生效**：检测到新 SW 安装完成会**自动重载一次**（sessionStorage 防重复循环），无需手动清缓存。
- localStorage 里的 V9.1 缓存若结构过期（无 `week_detail` 明细字段）会**自动丢弃**，不会被旧数据顶掉。

> 背景：v0.5 的 SW 对 `index.html` 用的是「缓存优先」且缓存名固定，导致只改页面/数据、没改 `sw.js` 时，老用户刷新拿到的仍是旧页面。v0.6 已修正为网络优先 + 缓存名升级（旧 `gzt-v1` 自动清除）。
> 万一仍看到旧界面，按 `Ctrl + F5`（Mac `Cmd + Shift + R`）强制刷新一次即可。

## 已交付模块

| 模块 | 状态 | 说明 |
|---|---|---|
| 今日要处理（置顶） | ✅ | 自动列出逾期/今天/3天内到期；逾期红框+一键顺延 |
| 资讯雷达 | ✅ | 命中重点关注词自动标红置顶；手动添加+JSON 批量导入 |
| 待办 & 排期 | ✅ | 月历+选中日清单+全部未完成；今天/选中用黄橙渐变高亮 |
| 粘贴钉钉/微信待办 | ✅ | 整段粘钉钉待办卡片，块状解析标题/截止/负责人/优先级 |
| 出入 · 兜底登记 | ✅ | 手动登记+JSON 批量导入（V9.1 端不可直拉时的兜底）；¥ + $ 双币种 |
| **V9.1 物管数据看板** | ✅ | 5 个子板块：库存货值 / 出入库·聚合 / **出入库·明细** / 仓储费 / 查询追踪；明细含项目号/合同号/型号/数量/金额/单价/方向/日期，本周&本月切换 |
| 选题灵感库 | ✅ | 灵感/待写/已发布 三态；公文体草稿一键生成 |
| 数据备份 | ✅ | 首屏+移动顶栏均有导出/导入；清空二次确认；待办可导出 CSV |
| PWA | ✅ | Service Worker 离线壳；iPhone Safari/安卓 Chrome 可"添加到主屏幕"当 App |

## 三处坦白

1. **微信/钉钉待办**做不到真"抓取"——端内数据没开放 API。用「粘贴消息自动解析」代替，实际能用。
2. **资讯自动抓取**做不到——纯静态跨域被禁。留了标准 JSON 导入接口，配调度任务可每日自动刷新。
3. **V9.1 数据直连**做不到——纯静态页面没有后端、没有金山文档凭据、跨域被禁。改为「生成期聚合 + data.json 静态分发 + 浏览器端 fetch」方案；每次要看最新数据需在带凭据环境跑一次 `gen_data.py` 后重部署。

## 手机装到桌面

**iPhone：** Safari 打开链接 → 底部「分享」↑ → 向下找「添加到主屏幕」→ 命名「牛马工作台」。
**安卓：** Chrome / 夸克打开 → 右上角 ⋮ → 「添加到主屏幕」/「安装应用」。
**电脑：** Chrome / Edge 地址栏右侧出现安装图标 ⌂，点击即可独立窗口运行。

## 后续可迭代方向

1. **自动刷新 V9.1 data.json**——云端跑每日定时任务（带 kdocs 凭据）：cron 调 `gen_data.py` → 覆盖 `dist/data.json` → 自动重部署，彻底免人工。
2. **出入库明细导出 CSV**——给明细表加一键导出，方便贴邮件/钉钉/汇报。
3. **多仓库库存 vs 出入库交叉**——同一仓库的"库存量、出入流量、仓储费"三表合并看板，定位周转效率低的仓库。
